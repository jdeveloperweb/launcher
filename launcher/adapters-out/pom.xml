<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.prognum</groupId>
    <artifactId>launcher-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <relativePath>../../pom.xml</relativePath>
  </parent>

  <artifactId>launcher-adapters-out</artifactId>
  <name>launcher-adapters-out</name>
  <description>Adapters de saida (driven): JDBC multi-banco, ProgramExecutor, proxy W_COP, stores in-memory, FeatureRegistry, PasswordVerifier.</description>

  <dependencies>
    <dependency>
      <groupId>com.prognum</groupId>
      <artifactId>common-environment</artifactId>
    </dependency>
    <dependency>
      <groupId>com.prognum</groupId>
      <artifactId>common-crypto</artifactId>
    </dependency>
    <dependency>
      <groupId>com.prognum</groupId>
      <artifactId>launcher-application</artifactId>
    </dependency>
    <dependency>
      <groupId>com.prognum</groupId>
      <artifactId>launcher-domain</artifactId>
    </dependency>

    <!-- Spring (context/beans/@Value) + cliente HTTP para a Rota C, sem servidor embarcado. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-web</artifactId>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
    </dependency>

    <!-- (JNA/oserver movido p/ o pascal-executor: o launcher e Java PURO, chama a execucao por REST) -->

    <!-- Redis (Lettuce): sessao distribuida + contador de tentativas (cache do VALIDA). -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-redis</artifactId>
    </dependency>
    <!-- (auth movido para o scci-core: SMTP/bcrypt/md5crypt agora vivem em scci-core-acesso/notificacao) -->

    <!-- Drivers JDBC: a conexao de cada ambiente vem do launcherenv.ini (DRIVERNAME). -->
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
    </dependency>
    <dependency>
      <groupId>org.firebirdsql.jdbc</groupId>
      <artifactId>jaybird</artifactId>
    </dependency>
    <dependency>
      <groupId>com.oracle.database.jdbc</groupId>
      <artifactId>ojdbc11</artifactId>
    </dependency>
    <dependency>
      <groupId>com.microsoft.sqlserver</groupId>
      <artifactId>mssql-jdbc</artifactId>
    </dependency>

    <dependency>
      <groupId>net.logstash.logback</groupId>
      <artifactId>logstash-logback-encoder</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
